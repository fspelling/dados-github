name: CD Fake

on:
  workflow_call:
    inputs:
      ambiente:
        type: string
        default: dev
        required: true

jobs:
  CD:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy no ambiente
        run: echo "Executando o deploy no ambiente ${{ inputs.ambiente }}"
